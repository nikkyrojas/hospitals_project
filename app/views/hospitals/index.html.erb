<h1> Hospitals </h1>
    <div class="doctors_link">
    <a href="/doctors">All Doctors</a>
    </div> 
    
    <div class="new_hospital_link">
    <%= link_to "Add Hospital", "/hospitals/new" %>
    </div> 
</p>

<ul id="hospitals"> 
    <% @hospitals.each_with_index do |hospital, index| %>
        <li id=<%= "#{index}"%>>
            <h3><%= link_to hospital.name, "/hospitals/#{hospital.id}" %> <%= button_to "Update #{hospital.name}", "/hospitals/#{hospital.id}/update", method: :get %></h3>
            <h6><%= hospital.created_at %></h6>
            <p>Phone: (303) <%= hospital.phone %></p>
            <p>Location City: <%= hospital.location %></p>
            
        </li>
    <% end %>
</ul>

